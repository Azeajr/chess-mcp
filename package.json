{
  "name": "chess-mcp-monorepo",
  "version": "0.0.0",
  "private": true,
  "description": "Chess repertoire UI + Node MCP server (TypeScript). Python servers live in server/.",
  "scripts": {
    "dev": "pnpm --filter @chess-mcp/ui dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "playwright": "^1.61.0"
  },
  "packageManager": "pnpm@11.3.0"
}
