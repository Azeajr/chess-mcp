{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "docker compose up -d && timeout 15 bash -c 'until curl -s -o /dev/null http://localhost:8000/ 2>/dev/null; do sleep 0.5; done'"
          }
        ]
      }
    ]
  },
  "enabledMcpjsonServers": [
    "chess-analysis",
    "chess-files"
  ]
}
