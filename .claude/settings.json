{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "docker compose up -d && timeout 15 bash -c 'until curl -sf http://localhost:8000/ > /dev/null 2>&1; do sleep 0.5; done'"
          }
        ]
      }
    ]
  },
  "enabledMcpjsonServers": [
    "chess-analysis",
    "chess-files"
  ]
}
