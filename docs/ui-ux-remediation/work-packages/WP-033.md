# WP-033 — Persistent Strategic Fit stage model

## Objective and findings

**Objective.** Give the workspace one progression model at every width, and remove the duplicated resolution rendering.

**Audit findings addressed.** UX-033

## Dependencies and gates

**Dependencies.** WP-007, WP-031

**Blocking gates.** None. DV-5 is fixed to three columns plus a decide region. **Completion gate.** AG-1 rerun, resolved only by its deterministic browser and real-AT command.

**Blocks.** WP-035

**Parallel with.** WP-034, WP-036

## Current behavior

`styles.css:1711–1713` sets `.strategic-fit-stage-nav { display: none }`; `:4613` restores it as a grid inside `@media (max-width: 820px)`. `StrategicFitWorkspace.tsx:128, 349–352` computes `usesStageTabs` from `matchMedia("(max-width: 820px)")` and switches roles between `tablist`/`tab`/`tabpanel` and plain `region`s. Verified at 1440×900: three panes visible, the resolution pane `display: none`, and resolution actions duplicated into the evidence pane by `:630–650` while `:672` renders them again for the compact tier. The workspace also implements its own focus trap rather than using the `Dialog` primitive from `WP-007`.

## Target behavior

A persistent stage indicator at all widths showing `Overview → Findings → Evidence → Decide` with the current stage marked and completed stages indicated. Wide viewports keep three visible columns but gain a fourth decide column or a persistent decide region — resolved by `DV-5`. Resolution actions are rendered once.

## Relevant files and symbols

- `styles.css:1711–1713`
- `strategic-fit-accessibility.spec.ts`

## Implementation approach

Extract the duplicated `ResolutionActions`/`TrainException`/`CohortEditor` block into one `ResolutionPanel` component rendered in exactly one place per tier. Replace `usesStageTabs` with a single stage model that renders a progress strip at all widths and switches only the _panel visibility_ strategy by tier. Migrate the workspace's focus trap onto `Dialog` (`nested` support already required for the Replacement Lab).
**DV-5 decision.** Keep three columns and render the decide region at the bottom of the evidence column, with the stage strip making progression explicit. Automation asserts geometry, stage-state equality, one resolution render, and overflow; layout preference is not claimed.

## Behaviors to preserve

Every Strategic Fit accessibility behaviour verified today: focus trap, `Escape`, focus restoration, `inert` + `aria-hidden` on `.app-main`, the roving-tabindex tablist with `Home`/`End` in the compact tier, print/export mode, reduced motion, forced colors, and the chart/table fallbacks. The 22 existing specs are the gate. The resolution-blocked message when the report is `stale` (`StrategicFitWorkspace.tsx:224–233, 651–655`).

## Acceptance criteria

- A stage indicator is visible at 390×844, 820×1000, 1101×800, and 1440×900, showing the current stage.

- `ResolutionActions`, `TrainException`, and `CohortEditor` each appear exactly once in the DOM at every width.

- The stale-report resolution-blocked message appears at every width.

- The compact tier's tablist keyboard behaviour (`←`/`→`/`Home`/`End`, roving tabindex, `aria-selected`) is unchanged.

- The workspace uses the `Dialog` primitive and satisfies its full contract; the Replacement Lab still nests correctly with `Escape` closing the Lab first.

- All 22 Strategic Fit specs pass, including `strategic-fit-accessibility.spec.ts`.

## Automated completion validation

**Automated tests.** Duplicate-render assertions; stage-indicator presence at four widths; the existing suite as the regression gate; the `Dialog` contract suite extended to the workspace and the Lab.

**Automated validation.** The package must satisfy its `requiredAutomatedValidation` contract in `manifest.json`; deterministic failure, missing evidence, or an inconclusive result blocks completion.

## Failure and rollback contract

**Planned risk.** Medium — the app's best-tested surface

Highest risk in the Strategic Fit workstream. Mitigation: split into two PRs — (a) de-duplicate the resolution block and add the stage strip, (b) migrate to `Dialog` — so a focus regression can be reverted without losing the stage work.

---

## Definition of Done

11. `pnpm lint` and `pnpm format:check` pass.
12. `pnpm -r typecheck` passes.
13. `pnpm docs:check` and `pnpm check:skills` pass.
14. `pnpm check:content` passes (from `WP-024` onward).
15. `pnpm --filter @chess-mcp/ui test:chat` passes.
16. `pnpm exec playwright test --config apps/ui/playwright.config.ts` passes on Chromium, Firefox, and WebKit.
17. Every acceptance criterion in the package has a named test that asserts it, and that test is green.
18. The baseline check this package was meant to flip is no longer `test.fixme` and passes.
19. All 22 Strategic Fit specs and all 43 store suites pass unchanged, or every change is individually justified in the PR description.
20. Any Strategic Fit snapshot update carries a written note stating what changed and why. Blanket snapshot updates are not acceptable.
21. Density guards pass: no panel grew more than its package's stated bound.
22. The manifest's `requiredAutomatedValidation` contract is covered by named green assertions, and their command results are recorded.
23. Every completion gate reports `confirmed-pass`; missing, unsupported, disagreeing, or inconclusive evidence fails closed without human approval.
24. The rollback note in the PR description states exactly what reverting does to user data. For `WP-004` and `WP-005` this is mandatory.
25. `docs/ui-ux-remediation-plan.md` §7 is updated: the finding's "Status after roadmap" moves to Closed and any "pending" gate note is removed.
26. If the package changed a documented command, `AGENTS.md` is updated.
