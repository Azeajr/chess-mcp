# Decision records

Create one Markdown record here for each resolved `AG-*`, `DV-*`, or `PD-*` gate. Record the gate ID, date, evidence, decision, and the `state.json` update that makes the gate `resolved`. Do not mark a gate resolved without linking its evidence.
