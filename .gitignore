.env
logs/

# node / pnpm
node_modules/
dist/
*.tsbuildinfo
.eslintcache
.vite/
apps/ui/test-results/
apps/ui/playwright-report/

# copied browser engine + ECO table (regenerated by predev/prebuild)
apps/ui/public/engine/
apps/ui/public/openings.tsv

# personal repertoire working data — not repo material. A clean smoke fixture
# lives in apps/mcp-server/test/fixtures/repertoires/ instead.
/repertoires/

# agent worktree checkouts
.claude/worktrees/
/.worktrees/

# per-developer Claude Code permission allowlist — machine-local, never shared
.claude/settings.local.json

# Interactive UX review sessions and evidence (not regression baselines)
/.ux-review/

# Personal repertoire exports used as review input. Committed fixtures are
# eval-stripped and anonymized under apps/mcp-server/test/fixtures/ instead.
/*.pgn

# Retained gate/validation logs; kept out of the tree but survive a reboot
/logs/

# Hermes agent worktrees — worker checkouts, never committed
.worktrees/
