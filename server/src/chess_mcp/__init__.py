"""chess-mcp: a grounded chess-analysis MCP server (Stockfish + python-chess).

The package bundles the analysis server (`chess_mcp.server`) and its engine-free domain
modules (structure, repertoire, openings, evalcache, apiclient, boardwidget) plus the
host-side file proxy (`chess_mcp.files`). Run with `chess-mcp` / `python -m chess_mcp`.
"""

__version__ = "0.3.0"
