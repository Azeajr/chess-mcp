[project]
name = "chess-mcp"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "mcp[cli]>=1.27.2",
    "chess>=1.11.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
