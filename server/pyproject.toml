[project]
name = "chess-mcp"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.0.0",
    "chess>=1.10.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
